<UserControl x:Class="WPF_MailSender.Components.ButtonsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:locals="clr-namespace:WPF_MailSender.Components"
             mc:Ignorable="d" d:DesignWidth="400" Height="65">
    <DockPanel DataContext="{Binding RelativeSource={RelativeSource AncestorType=locals:ButtonsControl}}">
        <UniformGrid Rows="1" Margin="5,0,5,0">

            <Button Command="{Binding CreateCommand}" Name="NewSender" Height="30" Width="100" HorizontalAlignment="Left" VerticalAlignment="Center" >
                <Image Source="/Img/New.png"></Image>
            </Button>

            <Button Command="{Binding DeleteCommand}" Name="DeleteSender" Height="30" Width="100" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image Source="/Img/delete.png"/>
            </Button>

            <Button Command="{Binding EditCommand}" Name="EditSender" Height="30" Width="100" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Image Source="/Img/Edit.png"></Image>
            </Button>

        </UniformGrid>
    </DockPanel>
</UserControl>
